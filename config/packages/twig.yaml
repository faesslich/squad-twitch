twig:
    default_path: '%kernel.project_dir%/templates'
    form_themes: ['forms/tailwind_3_layout.html.twig']
    paths:
        '%kernel.project_dir%/templates': App
when@test:
    twig:
        strict_variables: true
